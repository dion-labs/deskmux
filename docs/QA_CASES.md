# DeskMux risk-based QA registry

Stable IDs extend the original burn register; never renumber when adding cases. Scope comes from README.md, docs/development.md and the current source. No separate vision or journal existed at intake; TOKEN_BURN_2026-09-22.md is the checkpoint journal.

Status: **A** means automated evidence from 2026-09-22: baseline e9e6ac0 (103 tests), v0.2.2 (111 tests), expanded v0.2.3/v0.2.4 (114 tests), v0.2.5 (123 tests; nine focused runtime/log cases also pass Thread Sanitizer), v0.2.6 (143 tests; 21 receiver-filter functions also pass Thread Sanitizer), v0.2.7 candidate (150 tests; seven runtime-log functions cover both streams and pass Thread Sanitizer); **P** means partial automation with manual acceptance still NOT RUN; **M** means manual NOT RUN. Names below resolve under Tests/; a unit test proves only its stated boundary. Priority P0 covers input safety/authentication/install integrity; P1 covers recovery/privacy/routing; P2 covers presentation/experimental features. Run synthetic fixtures only. Never activate hardware probes or replace/restart live apps as part of baseline.

| ID | Priority / class | Procedure and expected outcome | Automated mapping / remaining manual gate | Status |
|---|---|---|---|---|
| DM-001 | P0 happy/error/privacy | Match pairing code, then mismatch/replay/reflection; only authenticated ordered messages accepted | SecureInputFrameCodecTests: wrongKeyAndReplayAreRejected, reflectedFramesCannotBeOpenedByTheirSender; InputPeerConnectionTests: encryptedPeerHandshakeWorksOverARealLoopbackSocket; real paired-Mac discovery remains manual | P |
| DM-002 | P0 error/platform | Deny then revoke Accessibility/Input Monitoring; local keyboard stays usable and guide targets stable bundled agent | CursorHandoffFeedbackTests permissionHelper tests and MacInputMonitorTests zeroDurationMonitorIsANoOpWithoutPermissions do not simulate revocation; System Settings acceptance manual | P |
| DM-003 | P0 recovery | Disconnect mid-relay with modifiers held; ownership and keys return local without inherited Command shortcuts | InputOwnershipTests and MacKeyboardModifierStateTests; actual event tap/release manual | P |
| DM-004 | P0 recovery | Press Control–Option–Command–Escape during relay; capture ends immediately | Physical source keyboard on two disposable Macs | M |
| DM-005 | P0 error | Destination return channel absent or native switch fails; handoff refuses or restores keyboard locally | InputOwnershipTests unavailable destination is partial; native Logitech failure path manual | P |
| DM-006 | P1 happy/error | Modifier/dwell at each outer edge, jitter, retreat, rearm; exactly one intentional handoff | MacPointerEdgeMonitorTests all detector cases; real mouse and both layouts manual | P |
| DM-007 | P0 recovery | Warm heartbeat reply expires, reconnect; warm state never starts input capture | warmHeartbeatWaitsForPongAndExpiresMissingReply, warmHeartbeatRecordsRoundTripAndAdvancesNonce; two-peer reconnect/capture manual | P |
| DM-008 | P1 happy/privacy | Copy text both ways, duplicate ID, UTF-8 limit; no echo and payload over 1 MiB stays local | clipboardUpdatesEnforceThePlainTextPayloadLimit covers byte bound only; duplicate/echo/actual pasteboard manual | P |
| DM-009 | P0 error/recovery | Missing DDC helper or invalid/unstable readback; report failure, no false success or automatic rollback | PhysicalMonitorTests parse/mapping/identity; three reads at three-second intervals and timeout require exact monitor | P |
| DM-010 | P1 recovery | Evacuate eligible hidden-display windows, then move/resize before return; preserve user changes and exclude unsupported windows | VisibleScreenWindowsTests geometry and unchanged bounds only; Accessibility/window classification manual | P |
| DM-011 | P0 error/privacy | Corrupt hash, wrong signer/bundle, older build or invalid archive; reject before replacing app | No installer rejection integration tests; isolated signed disposable bundles required | M |
| DM-012 | P0 happy/recovery | Valid newer update from Downloads and canonical path; backup, receipt, canonical relaunch, grants survive | InputPeerConnectionTests updatesMigrateBootstrapCopiesIntoUserApplications, updatesRecognizeAnExistingCanonicalInstallation, stableAgentUpdatesResolveTheirEnclosingDeskMuxApplication cover layout only; installation manual | P |
| DM-013 | P2 platform/recovery | Start/stop virtual display and disconnect viewer; no stuck capture/display | DeskMuxVirtualDisplayTests validates config only; runtime and Screen Recording permission manual | P |
| DM-014 | P1 privacy | Synthetic inventory includes serials/addresses; sanitized result omits them; inspect other logs for secrets/text/keys | DiagnosticsParserTests and LocalSnapshotTests; motionTraceRecorderRejectsEveryNonMotionInputKind; clipboard metadata sink now covered by DM-043; network/handoff persistence/schema now covered by DM-052–055; freeform details/errors are not universally redacted, live log flows still manual | P |
| DM-015 | P2 platform | First-run, menu, settings, keyboard navigation, larger text; readable reachable controls and accurate capability | VoiceOver/keyboard/appearance acceptance manual | M |
| DM-016 | P0 error | Input frame prefix exceeds cap; reject before awaiting body; truncated prefix/body never emits a message | inputParserRejectsOversizedHeaderAndBuffersIncompleteFrames; see burn evidence | A |
| DM-017 | P0 error/recovery | Alter authenticated ciphertext or reorder input; reject without advancing receiver sequence; authentic next message still decodes | inputAuthenticationAndOrderFailuresDoNotAdvanceSequence; see burn evidence | A |
| DM-018 | P1 error/privacy | Screen wire replay/reflection/tamper rejected; partial/coalesced frames remain ordered | ScreenWireProtocolTests: screenAuthenticationReplayAndReflectionFailWithoutConsumingSequence and fragmented round trip/wrong key | A |
| DM-019 | P1 error | Oversized screen frame rejected at header or seal; later valid seal remains sequence zero | screenParserRejectsOversizedHeaderAndBuffersIncompleteFrames, screenOversizedSealDoesNotConsumeSequence | A |
| DM-020 | P1 privacy | Rich text, images, files and promises remain local; initial old pasteboard not broadcast merely on launch | Disposable peer pasteboards; real clipboard intentionally untouched during burn | M |
| DM-021 | P0 error | Unknown/conflicting learned route or ambiguous controller; no driver writes | ProfilePlannerTests and planningRejectionCallsNoDriverOperations | A |
| DM-022 | P0 recovery | Partial monitor switch failure; safe ordered phases and honest partial result | TransactionEngineTests transactionCompletesInSafePhases, partialSwitchFailureIsReportedWithoutFalseRollback | A |
| DM-023 | P1 error | Passive/unobserved route evidence cannot overwrite a confirmed route | RouteRegistryTests | A |
| DM-024 | P1 platform | Bonjour duplicate names, stale routes, legacy status and malformed TXT; deterministic peer ranking and compatible decode | InputPeerConnectionTests newestBonjourCollisionGetsHighestRank, peerToPeerRouteOutranksAStaleInfrastructureRoute, newMenuCanDecodeStatusFromAnOlderAgentBuild, incompleteOrMislabeledVersionAdvertisementIsNotTrusted | A |
| DM-025 | P1 recovery | Receiver stalls acknowledgements; bound sender queue, coalesce motion without losing reliable input | senderStopsAddingToNetworkQueueUntilReceiverAcknowledges, pointerMotionIsCoalescedBeforeReliableInputWithoutLosingDeltas | A |
| DM-026 | P1 error/recovery | Lose motion datagram; cumulative state recovers and tampering rejected without reliable sequence gaps | SecureInputFrameCodecTests pointerMotionDatagramsRecoverStateAfterLossAndRejectTampering; InputPeerConnectionTests datagram cases | A |
| DM-027 | P1 privacy/recovery | Disable competing Flow; preserve unrelated settings, backup exact original, repeat safely | LogitechFlowConfigurationTests synthetic temp file backup/idempotence; installed Options+ behavior manual | P |
| DM-028 | P1 platform | AOC absent/duplicate or peer has different UUID; choose exactly one locally identified display or issue no switch | monitorIdentityIsPeerLocalAndRejectsAmbiguousDisplays; MacBook HDMI return and visible signal manual | P |
| DM-029 | P1 recovery | Follow owner changes while busy/disabled/unknown; debounce latest owner and avoid loops | monitorFollowDebouncesAndKeepsOnlyLatestOwner, monitorFollowNeverActsWhenDisabledOrOwnerUnknown; physical follow manual | P |
| DM-030 | P1 platform | Open Your desk from menu, close and reopen, after coordinated restart; visible independent window each time | SwiftUI openWindow source reviewed; desktop UI acceptance manual | M |
| DM-031 | P1 platform | macOS 14 minimum and current supported macOS; Apple silicon signed archive starts with correct helpers/resources | Release build/codesign/archive plus fresh isolated-machine launch; older OS unverified | M |
| DM-032 | P0 recovery | Sleep/wake, Wi-Fi loss, peer quit, source quit during held keys; local control recovers and reconnect remains idle | Coordinated two-Mac live session matrix; no restart authorized this burn | M |
| DM-033 | P1 platform | Launch at login enabled/disabled; stable service starts once and permissions survive upgrade | Disposable login session; no logout/restart this burn | M |
| DM-034 | P1 recovery | Update extraction/copy fails or disk fills; installed bundle preserved/recovered with actionable receipt | Disposable filesystem and signed bundle integration harness needed; never fill real disk | M |
| DM-035 | P2 platform | Negative display origins and corners; cue stays visible and internal monitor boundaries do not hand off | CursorHandoffFeedbackTests and MacPointerEdgeMonitorTests negativeDisplayOrigin/outermostMonitorBoundary/cornersDoNotTrigger | A |
| DM-036 | P1 privacy/recovery | Copy text then unsupported/oversized content; reconnect must not replay old observed text; remote application suppresses echo and new local text recovers | ClipboardObservationTests synthetic generation state; live two-Mac pasteboard and reconnect still manual | P |
| DM-037 | P1 privacy/recovery | Pause reconnect replay, observe unsupported/oversized or remote replacement, then resume; no old callback fires, next eligible text still delivers, removal stops future delivery | ClipboardObservationTests queued-replay and serial-removal tests exercise production delivery scheduler with synthetic closures; no pasteboard access | A |
| DM-038 | P1 platform | Compile, test, icon-smoke, package and verify signature using CI Swift 6.2.4/macOS 15 as well as local Swift 6.3.2; actor isolation must compile on both | GitHub run 35697374677 passed on Swift 6.2.4; local Swift 6.3.2 suite/package passed. v0.2.3 failed the weak-capture check; v0.2.4 corrects it | A |
| DM-039 | P1 recovery/order | Send 100 ordered encrypted messages, cancel peer, reconnect with fresh codec state on same loopback listener; no previous-session payload enters new connection | InputTransportLifecycleTests.loopbackTransportOrdersFramesAndReconnectsWithFreshSequence; actual NWConnection, localhost only | A |
| DM-040 | P0 error/recovery | Wrong key closes transport without delivering plaintext; a throwing consumer closes authenticated transport; next correct connection works | loopbackTransportRejectsWrongKeyAndAcceptsNextConnection, loopbackTransportClosesWhenConsumerRejectsAuthenticatedMessage; fixture consumer policy is not production receiver identity validation | A |
| DM-041 | P0 cancel | Pause network callbacks, cancel, then try another send; reject immediately before asynchronous cancellation callback | loopbackCancelImmediatelyRejectsFurtherSends; failed on v0.2.4 then passes after synchronous send-gate fix | A |
| DM-042 | P1 concurrency/order | Eight concurrent producers enqueue 800 encrypted messages; receiver accepts every unique message without sequence failure | loopbackConcurrentProducersPreserveEncryptedSequence; v0.2.4 failed EPROTO, seal+enqueue serialization fixes it | A |
| DM-043 | P1 privacy/persistence/recovery | Persist clipboard metadata, recreate writer, append concurrently; valid whole JSONL records, no text fields/content; blocked destination fails harmlessly and resumes after repair | ClipboardEventLogTests using production asynchronous logger and disposable directories; network/handoff logs still unisolated/manual | A |
| DM-044 | P1 framing/platform | Send 1 MiB synthetic UTF-8 clipboard payload across real 64 KiB receive chunks, acknowledge same update ID; byte-identical decoded message and encrypted reply | loopbackClipboardPayloadSurvivesChunkingAndAcknowledgement; no pasteboard or real clipboard bridge | A |

| DM-045 | P0 admission | Reject input before hello/begin, repeated hello, duplicate begin, unsupported directions and protocol versions | ReceiverSessionAcceptanceTests exercises actual ReceiverSession over encrypted localhost transport with entirely fake effects; native UUID/CGEvent validation remains separate | A |
| DM-046 | P0 lifecycle/recovery | Disconnect/stop releases fake held input and unregisters once; decoded and encrypted post-goodbye tails cannot inject/register; local return prevents later input | receiverHandshakeInputAndDisconnectReleaseHeldStateOnce, receiverTerminalSessionRejectsAlreadyDecodedTailFrame, receiverEncryptedGoodbyeBatchCannotApplyTailEffects, receiverLocalReturnPreventsLaterInputFromIgnoringPeer; physical release still manual | A |
| DM-047 | P0 concurrency | Pause input factory, stop receiver, resume factory; no adoption/register/modifier publication after cleanup | receiverStopDuringInputFactoryCannotRegisterAfterCleanup; no native input factory invoked | A |
| DM-048 | P1 clipboard/recovery | Require input-purpose hello, matching source and size limit; ACK only after fake application; old completion cannot ACK into fresh connection | ReceiverSessionAcceptanceTests clipboard fixtures; actual pasteboards and echo suppression remain manual | A |
| DM-049 | P0 rejection | Sink rejection closes connection and cleans prior held state; installer invocation requires explicit update capability | ReceiverSessionAcceptanceTests uses fake contract errors and fake installer result; does not certify native packet validation or installation | A |

| DM-050 | P0 motion lifecycle | Real motion registry rejects fake motion after local return/stop; in-flight motion completes before terminal release | receiverMotionCannotContinueAfterReturnOrStop, receiverMotionInFlightCompletesBeforeTerminalRelease; no UDP listener or physical pointer movement | A |
| DM-051 | P0 update admission | Atomically admit authorized update before stop; admitted blocking fake installer may finish, new admission rejected and no result sent after stop | receiverAdmittedInstallerCanFinishAfterStopWithoutNewAdmission; actual installer remains manual | A |

| DM-052 | P1 persistence/order | Append 100 ordered records across writer recreation; eight writers attempt 320 records per stream; persisted records remain whole, unique and ordered per producer; contention may drop records | RuntimeEventLogTests with explicit temporary directories and actual network/handoff files; unchanged baseline corrupts JSON, nonblocking append/record-lock fix tested | A |
| DM-053 | P1 recovery/rotation | Rename active log between completed appends, then delete replacement; next records recreate active path without changing archived records | runtimeLogReopensAfterExternalRotationAndRemoval; existing external rotation behavior only, no automatic rotation policy | A |
| DM-054 | P1 error/recovery | File blocks directory or directory blocks log path; logging fails harmlessly without changing blocker, then recovers after fixture repairs destination | runtimeLogRecoversFromBlockedDirectoryWithoutChangingBlocker, runtimeLogRecoversWhenLogPathWasADirectory; synthetic disposable paths | A |
| DM-055 | P1 metadata/privacy boundary | Unicode/newline diagnostic canary remains one JSON record with existing key allowlist; cannot forge top-level clipboard/key fields | runtimeLogSchemaAndEscapingDoNotInventPrivateFields; detail/error are still verbatim diagnostics, not redacted; does not certify actual service inputs are free of private data | P |

| DM-056 | P0 diagnostic noninterference | Owned temporary Python process holds log lock; actual writer returns within deadline, skips contended record, and resumes persistence after unlock | runtimeLogHeldProcessLockDoesNotDelayCallerAndRecovers for both network/handoff; synchronous blocking-lock candidate failed, nonblocking best-effort fix passes | A |

## Reproduction and acceptance receipt

Baseline: `swift test --scratch-path .build/public-preview` (wrap with the coordinated resource-run.py for this burn). Filtered suites use `--filter` plus test-name regex. Record date, Git revision and dirty diff, command, exit status, test count and raw log. All newly added cases require a fresh run regardless of baseline age.

Manual receipt per ID: date; app version/build and both Mac/OS models; disposable fixtures; preconditions/permissions; exact actions; expected and observed result; screenshots or sanitized logs; recovery action; PASS/FAIL/BLOCKED. DDC readback and visible image are separate observations. No unit test can certify MacBook switching or window opening after restart. Stop on lost local control, unexpected personal data access, or ambiguous device identity.
